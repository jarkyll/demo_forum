%h1= @post.title
%p= @post.content
-# looks like = switches to ruby code

= link_to "Edit", edit_post_path(@post)
= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Do you want to delete this?"}
= link_to "Home", root_path
-# link_to creates a link and edit_post_path does the method
-# look up more into the delete part
-# you pass the route as the first argument
-# :method and :data-confirm are HTML5 attributes
-# method attribute says what REST call is being dong aka delete
