%h1= @post.title
%p= @post.content
-# looks like = switches to ruby code

= link_to "Edit", edit_post_path(@post)
= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Do you want to delete this?"}
= link_to "Home", root_path
