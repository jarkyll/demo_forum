- @posts.each do |post|
  %h2= link_to post.title, post
  %p
    Published at
    = time_ago_in_words(post.created_at)
    by
    = post.user.email
-# we need to add the feature of replying to comments
-# upvotes and downvotes
-# idk
