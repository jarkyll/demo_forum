.comment
  %p= comment.comment
  %p= comment.user.email
  = link_to "Edit", edit_post_comment_path(comment.post, comment)
  = link_to "Delete Reply", [comment.post, comment], method: :delete, data: {confirm: "Are you Sure?"}
-# so the method: asks for which method to do
-# the data is the pop up for what will happen
-# the [] is
-# for edit, we are grabbing the comment.post and the comment
